apiVersion: v1
kind: Pod
metadata: 
  name: service-lb-demo
  # these labels are used to register this pod with the service
  labels:
    project: roboshop
    component: frontend
    environment: dev
    purpose: load-balancer
spec:
  containers:
  - name: nginx
    image: nginx